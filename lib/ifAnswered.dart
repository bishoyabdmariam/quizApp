import 'package:flutter/material.dart';

class IfAnswered extends StatelessWidget {
  const IfAnswered({super.key});

  @override
  Widget build(BuildContext context) {
    return DecoratedBox(
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(7),
        color: Colors.blue,
      ),
    );
  }
}
